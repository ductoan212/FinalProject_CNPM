@model FinalProject.Models.DS_MONHOC_MO

@{
    ViewBag.Title = "Create";
    Layout = "~/Areas/PDT/Views/Shared/LayoutPDT.cshtml";
}

<h2>Nhập danh sách môn học mở</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @*<h4>DS_MONHOC_MO</h4>*@
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @*<div class="form-group">
                @Html.LabelFor(model => model.MaMo, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.Di(model => model.MaMo, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.MaMo, "", new { @class = "text-danger" })
                </div>
            </div>*@

        <div class="form-group">
            Mã học kỳ
            <div class="col-md-10">
                @Html.EditorFor(model => model.MaHKNH, new { @class = "form-control" })
                @*<select class="form-control" id="MaHKNH">
                    @foreach(var item in ViewBag.ListHK)
                    {
                        <option value="@item.MaHKNH">HK @item.HocKy (@item.Nam1-@item.Nam2)</option>
                    }
                </select>*@
                @Html.ValidationMessageFor(model => model.MaHKNH, "", new { @class = "text-danger" })
            </div>
        </div>
        @if (ViewBag.IsErr == 1)
        {
            <div class="text-danger">Không tồn tại mã học kỳ năm học</div>
        }
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Kế tiếp" class="btn btn-success" />
            </div>
        </div>
    </div>
}
<hr />
<div>
    @Html.ActionLink("Quay lại", "ListHK")
</div>
